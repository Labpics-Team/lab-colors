self-hosted-runner:
  labels:
    - labcolors-ephemeral

paths:
  .github/workflows/mutation.yml:
    ignore:
      # `queue` — валидная семантика GitHub Actions, которой ещё нет в схеме
      # поддерживаемого actionlint; удалить, когда linter начнёт принимать ключ.
      - 'unexpected key "queue" for "concurrency" section'
