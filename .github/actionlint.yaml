# Self-hosted лейблы сняты миграцией на GitHub-hosted раннеры (публичное репо):
# все jobs исполняются на эфемерных ubuntu-latest / macos-15.
paths:
  .github/workflows/mutation.yml:
    # actionlint 1.7.12 отстаёт от текущей GitHub-схемы concurrency.queue;
    # узкое исключение удаляется, когда линтер начнёт принимать `queue: max`.
    ignore:
      - 'unexpected key "queue" for "concurrency" section'
