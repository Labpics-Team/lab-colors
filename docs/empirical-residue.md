# Empirical residue — что осталось hand-picked и какой эксперимент это закрывает

Спутник `docs/empirical-inventory.md`. Волна `science/constants-objectivization`
классифицировала все **25** SSOT-TRACKED констант реестра ровно в один статус и
измерила ЭКСПОЗИЦИЮ каждой чувствительной: какая доля входного пространства меняет
классификационный выход, если константа пройдёт по своему интервалу. Это
превращает вопрос «вдруг оно красит некорректно СЕЙЧАС?» в измеренную величину и
приоритизирует эксперименты.

**Инвариант волны:** ноль изменений поведения. `reference-vectors` и
`chain-invariants` байт-идентичны; продакшн-константы НЕ тронуты; добавлены только
`#[cfg(test)]`-локи и exposure-тесты. Где строгий вывод даёт значение, ОТЛИЧНОЕ от
текущего, значение НЕ меняется — помечено «конфликт вывода». (Апдейт 2026-07-06:
единственный такой конфликт — `HUE_DRIFT_PENALTY_SLOPE` — разрешён по замеру,
кандидат отклонён; см. мишень №2.)

## Статусы

- **(a) DERIVABLE** — выводится из первопринципов/стандартов; derivation-identity
  тест (значение обязано остаться байт-идентичным).
- **(b) LITERATURE-GROUNDABLE** — есть опубликованное измерение; tolerance-тест.
- **(c) INTERVAL-INSENSITIVE** — задекларирован интервал; sensitivity/exposure
  доказывает, что выход стабилен, значит точное значение нематериально.
- **(d) CALIBRATION-REQUIRED** — невыводима и чувствительна; нужен эксперимент.
- **(e) DESIGN-CHOICE** — задекларирована и конфигурируема (#31).

Итог волны objectivization: (a)=4, (b)=0, (c)=7, (d)=6, (e)=8. **После follow-on
`science/floor-derivation` (мишени #3/#4, +`QUANT_GUARD` #53, +`MODEL_LC_FLOOR` #54)
по 27 константам: (a)=6, (b)=1, (c)=10, (d)=2, (e)=8.** Остаток (d) сузился до
ДВУХ высоко-экспозиционных мишеней (#52, #37), ждущих психофизики.

## Классификация всех 25 (+`QUANT_GUARD` +`MODEL_LC_FLOOR` = 27)

| # | константа | знач. | класс | провенанс / статус | лок / тест |
|---|-----------|-------|-------|--------------------|------------|
| 38 | `ACHROMATIC_CHROMA_EPS` | 1e-7 | (a) | представимость: шум 3.7e-8 < EPS < мин.хрома 1.06e-3 | `achromatic_eps_between_f64_noise_and_min_representable_chroma` |
| 18 | `LIGHTNESS_SETTLE` | 0.002 | (a) | меньше 1/256 (мин. шаг 8-бит сетки) | `lightness_settle_is_below_min_grid_step` |
| 39 | `HUE_DOMAIN_MIN_INCLUSIVE` | 0.0 | (a) | определение угла mod 360 | `hue_domain_is_the_circle_modulus_codomain` |
| 40 | `HUE_DOMAIN_MAX_EXCLUSIVE` | 360.0 | (a) | определение угла mod 360 | `hue_domain_is_the_circle_modulus_codomain` |
| 16 | `TINT_PERCEPTIBLE_MP_FLOOR` | 1.5 | (c) | потолок ахром. M-prime шума; экспозиция 0.07% | `tint_floor_tracks_achromatic_mp_noise_ceiling`, `exposure_tint_perceptible_mp_floor` |
| 47 | `ACHROMATIC_MP_THRESHOLD` | 5.0 | (c) | пустой зазор шум-хрома; экспозиция 1.99% | `achromatic_threshold_sits_in_empty_noise_to_chroma_gap`, `exposure_achromatic_mp_threshold` |
| 17 | `CUSP_HALF_WINDOW_DEG` | 40.0 | (c) | намеренный кап дрейфа (42.5 град) | `cusp_window_is_near_measured_gamut_drift` |
| 36 | `HUE_SEARCH_HALF_WINDOW` | 30.0 | (c) | нежёсткое окно/кап; экспозиция 0.93% | `exposure_hue_search_window` |
| 19 | `STRICT_STEP` | 0.5 | (c) | граница квантования сетки (не JND) | `strict_step_sits_just_above_typical_grid_step`, `exposure_strict_step` |
| 50 | `QUANT_BUDGET` | 1.0 | (c) | около 2x медианного Lc-шага; экспозиция 1.84% | `quant_budget_is_a_couple_of_grid_steps`, `exposure_quant_and_dj_budgets` |
| 51 | `DJ_BUDGET` | 0.6 | (c) | около 1.5x медианного dJ-prime шага; экспозиция 1.55% | `dj_budget_tracks_grid_step`, `exposure_quant_and_dj_budgets` |
| 52 | `PAIR_CROSSOVER_Y` | 0.30 | (d) | экспозиция 21.69% + 12 якорей labui | `crossover_side_is_invariant_across_palette_gap`, `exposure_pair_crossover` |
| 37 | `HUE_DRIFT_PENALTY_SLOPE` | 0.15 | (d) | экспозиция 14.29%; конфликт вывода РАЗРЕШЁН (хорда отклонена по замеру) | `exposure_hue_drift_penalty_slope` + `chord_derived_slope_rejected_degenerates_to_window_edge` |
| 1 | `DECORATIVE_FLOOR_MIN` | 7.5 | **(a)** | DERIVED = `MODEL_LC_FLOOR` 7.3 + `QUANT_GUARD` 0.2; 7.3 = (LO_CLIP−LO_BOW_OFFSET)×LC_SCALE из GROUNDED APCA-клипа (мишень #3 закрыта) | `decorative_floor_is_model_floor_plus_guard`, `no_pair_emits_contrast_below_model_floor` |
| 54 | `MODEL_LC_FLOOR` | 7.3 | **(a)** | DERIVED = (LO_CLIP−LO_BOW_OFFSET)×LC_SCALE = 7.3 — минимум ненулевого контраста модели (issue #44); литерал 7.3, тождество с формулой пиннится | `model_lc_floor_is_the_published_clip_minimum` |
| 35 | `IC_DECORATIVE_FLOOR_MIN` | 15.0 | **(b)** | GROUNDED — APCA Lc 15 (минимум различимости не-текста; draft/single-origin, не WCAG 3) (мишень #3 закрыта) | `ic_floor_is_apca_lc15_with_order_preserving_shift` |
| 53 | `QUANT_GUARD` | 0.2 | **(c)** | задекларир. запас над модельным полом 7.3; замер: min ненул. 7.3005, скачок клипа 7.85 (не покрывается, и не должен) | `decorative_floor_is_model_floor_plus_guard` |
| 48 | `HUE_PURITY_MP_REF_RATIO` | 1.5 | **(c)** | хроматич. инвариант (purity=1 при mp≥ref); форма мотивирована Abney (мишень #4) | `hue_purity_curve_shape_is_pinned` |
| 49 | `HUE_PURITY_EXPONENT` | 0.6 | **(c)** | форма — Abney 1909 / K-S-S 1984 (цит.); магнитуда калибр., дрейф лишь у near-нейтралей, max\|Δpurity\|=0.148 (мишень #4) | `exposure_hue_purity_curve` |
| 15 | `TINT_HUE_STIFFNESS` | 9.0 | (e) | конфиг `hue_stiffness` (валидируется) | `hue_stiffness_negative_is_rejected` |
| 13 | `NEUTRAL_TINT_RATIO` | 0.10 | (e) | конфиг custom-tint ratio | `custom_tint_overrides_hue_and_ratio` |
| 12 | `NEUTRAL_HUE_DEG` | 286.0 | (e) | конфиг custom-tint hue (замер по нейтрали) | `custom_tint_overrides_hue_and_ratio` |
| 21 | `CHROMA_FRACTION` | 0.88 | (e) | конфиг `chroma_fraction` (labui=1.0) | `chroma_fraction_out_of_bounds_is_rejected` |
| 20 | `DEFAULT_HARDNESS` | 5.0 | (e) | конфиг `hardness` (валидируется) | `hardness_below_one_is_rejected` |
| 22 | `NEUTRAL_DEFAULT_GAMMA_LIGHT` | 1.75 | (e) | `CurveParams` конфиг (`with_params`) | `custom_tint_overrides_hue_and_ratio` |
| 23 | `NEUTRAL_DEFAULT_GAMMA_DARK` | 1.5 | (e) | `CurveParams` конфиг (`with_params`) | — |
| 24 | `NEUTRAL_DEFAULT_CHROMA_PEAK_T` | 0.35 | (e) | `CurveParams` конфиг (`with_params`) | — |

**(b) после follow-on = 1 (было 0) — честно.** `IC_DECORATIVE_FLOOR_MIN` = 15.0
заземлён в опубликованном APCA-уровне Lc 15 (минимум различимости не-текста), с
явной оговоркой: это черновиковый (draft/beta), single-origin (Somers/Myndex)
дизайн-уровень — НЕ норматив WCAG 3 и не независимое психофизическое измерение
JND. `DECORATIVE_FLOOR_MIN` (7.5) — не (b), а (a): выводится алгебраически из
GROUNDED APCA-клипа, а не из отдельного опубликованного измерения. Прочие
калибровочные константы прямого опубликованного измерения ИМЕННО своего значения
по-прежнему не имеют; литературно-заземляемые `B0`/`BW`/`H_Y_DEG` — это M-строки
`cleanliness.rs` (волна Zone B), вне этого набора. Выдумывать цитату под
остальные — подлог.

## EXPOSURE-анализ

**Метод сетки:** «репрезентативная гамма» = sRGB 8-бит куб, сэмплированный каждое
8-е кодовое значение по каналу, 32^3 = **32768** представимых цветов
(`exposure_support::rgb_cube`). «Реальные входы» = **49** якорей labui из
замороженного паспорта `crates/labcolors-wasm/tests/data/labui.config.json`
(`exposure_support::LABUI_ANCHORS`). Флип-зона порогового theta = доля входа с
величиной в полосе `[lo, hi)`, где решение (величина < theta) меняется, пока theta
идёт по полосе. Все числа — снапшот воспроизводимых тестов
`cargo test -p labcolors-core exposure` (2026-07-06).

| константа | интервал свипа | флип гаммы | якоря labui в зоне | вердикт |
|-----------|----------------|-----------|--------------------|---------|
| `PAIR_CROSSOVER_Y` | Y в (0.246, 0.423) | **21.69%** | **12**: 34C759, 3E87FF, 409CFF, 4A8FFF, 5696FF, 7D7AFF, BF5AF2, DA8FFF, FF3B30, FF6161, FF6482, FF9008 | (d) МИШЕНЬ #1 |
| `HUE_DRIFT_PENALTY_SLOPE` | slope 0.10..0.20 | **14.29%** (dHue до 30 град) | n/a ((l,hue)-сетка) | (d) МИШЕНЬ #2; конфликт разрешён |
| `IC_DECORATIVE_FLOOR_MIN` | Lc +-25% | 25.0% (proxy) | n/a (Lc-сетка) | **(b) GROUNDED** — APCA Lc 15 (мишень #3 закрыта) |
| `DECORATIVE_FLOOR_MIN` | Lc +-25% | 12.5% (proxy) | n/a (Lc-сетка) | **(a) DERIVED** — 7.3 клип + 0.2 guard (мишень #3 закрыта) |
| `ACHROMATIC_MP_THRESHOLD` | M-prime в [2.61, 10.0] | 1.99% | 4 нейтрали: 3C3C43, 787880, B0B0B9, F6F8FA | (c) benign (подстановка нейтралей штатна) |
| `QUANT_BUDGET` | +-50% | 1.84% | n/a | (c) immaterial |
| `DJ_BUDGET` | +-50% | 1.55% | n/a | (c) immaterial |
| `HUE_SEARCH_HALF_WINDOW` | 25..45 град | 0.93% | n/a | (c) намеренный кап |
| `TINT_PERCEPTIBLE_MP_FLOOR` | M-prime в [0.75, 2.25] | **0.07%** | 1: FFFFFF | (c) immaterial |
| `CUSP_HALF_WINDOW_DEG` | — | намеренный кап (дрейф 42.5 > окно 40) | n/a | (c) кап |
| `STRICT_STEP` | Lc-шаг +-20% | 60% overlap* | n/a | (c) граница сетки, benign |
| `HUE_PURITY_MP_REF_RATIO` / `_EXPONENT` | показатель [0.4,0.9] | max\|Δpurity\|=0.148 (непрерывный, не флип) | n/a | **(c)** — хроматич. якоря инвариантны; форма — Abney (мишень #4 закрыта) |

\* STRICT_STEP: «60%» — доля соседних Lc-шагов серой сетки в +-20% вокруг
значения, т.е. STRICT_STEP сидит РОВНО в типичном шаге сетки (по дизайну, см.
лок). Это НЕ доля мис-окраски: STRICT_STEP — граница квантования, не перцептивный
порог, поэтому перекрытие ожидаемо и безвредно.

**Главный вывод экспозиции:** «красит ли некорректно сейчас» — почти нигде. Из
чувствительных только ДВЕ константы имеют существенную зону (>10%): полярностный
кроссовер пары `PAIR_CROSSOVER_Y` (21.69%, 12 реальных якорей) и наклон штрафа
дрейфа `HUE_DRIFT_PENALTY_SLOPE` (14.29%). Остальные (c) — не выше 2% или
намеренный кап. Важно: 21.69%/14.29% — доля, где значение РЕШАЕТ выход, а НЕ доля
«неправильного»; текущие значения дизайн-обоснованы, экспозиция лишь ставит
приоритет валидации.

## Остаток (d): протоколы экспериментов, приоритет по экспозиции

### 1. `PAIR_CROSSOVER_Y` = 0.30 — приоритет ВЫСШИЙ (21.69%, 12 якорей)

Провенанс сегодня: 10 Figma-якорей labui, 0 наблюдателей; консенсус-лок
подтверждает лишь чистый зазор для этих 10
(`crossover_side_is_invariant_across_palette_gap`).
- **Стимулы:** заливки-свотчи с Y в [0.18, 0.45] (шаг около 0.02), каждый показан
  с БЕЛЫМ и с ЧЕРНИЛЬНЫМ лейблом на WCAG-легальном контрасте; оттенок/хрома —
  репрезентативный набор фирменных семей.
- **Задача:** 2AFC — какой лейбл читается/предпочтительнее на свотче.
- **N >= 15** наблюдателей (нормальное цветовосприятие, калиброванный дисплей, D65).
- **Метод:** логистическая психометрическая подгонка P(белый) от Y; кроссовер = Y
  при P = 0.5 (PSE).
- **Стат-тест:** bootstrap 95% CI на PSE.
- **Критерий приёмки:** ширина CI < 0.03 Y; если PSE в текущем интервале
  (0.246, 0.423) — принять как значение, иначе эскалация владельцу.
- **Инструмент:** этот протокол реализован в `experiments/psychophysics/`
  (генератор стимулов из паспорта, офлайн-раннер 2AFC, логистическая MLE +
  bootstrap-вердикт, замок честности на синтетике). Готов к запуску; человеческая
  сессия за владельцем. Runbook — `experiments/psychophysics/README.md`.

#### Провенанс-апдейт (2026-07-06): APCA-сверка, hue-оговорка, число 0.179

Дополнение провенанс-записи (строка 52 реестра `docs/empirical-inventory.md`).
Харнесс протокола выше реализован в PR #170 (`experiments/psychophysics/`,
ветка `feat/psychophysics-harness`) — это эмпирический путь разрешения.

1. **Сверка с APCA-моделью.** Люминансное LPC-ядро (APCA-базис SAPC-8
   0.0.98G-4g) асимметрично: перелом «чёрный обгоняет белого» лежит около
   `Y ≈ 0.36` (V3-замер, зафиксирован в rustdoc `semantic.rs`: на фоне
   `Y = 0.211` белый набирает 69.78 Lc против 39.79 у чёрного). Расхождение с
   `PAIR_CROSSOVER_Y = 0.30` — **≈ 0.06**: модель предсказывает перелом выше.
   Противоречия с якорным зазором нет — 0.36 лежит внутри того же
   перцептивно-мотивированного интервала (0.246, 0.423); но выбор 0.30 —
   дизайн-решение НИЖЕ APCA-предсказания, а не его подтверждение.
2. **Hue-оговорка (Гельмгольц-Кольрауш).** Порог сравнивает чистый WCAG-Y
   канонического светлого якоря (`wcag_y_encoded < PAIR_CROSSOVER_Y`, `pair.rs`);
   hue/хрома-вклад в воспринимаемую яркость (H-K) в решение стороны не входит.
   Значение получено и валидируется на люминансной оси (якоря хроматичны, но
   ось решения нейтральна) — hue-зависимость кроссовера текущим значением НЕ
   покрыта. В ядре это осознанный скоуп: ADR-0003
   (`docs/decisions/0003-hk-scope.md`) переводит ось читаемости в люминансный
   домен `Ys` (H-K остаётся на яркостных осях), а согласование порогов
   `pair.rs` (0.30, 0.179) с `Ys`-доменом — его открытый вопрос п. 3.
   Протокол §1 (сетка Y на 10 семьях оттенков) даст первые данные о
   hue-зависимости PSE.
3. **Число 0.179 — что это и что это НЕ.** Все вхождения 0.179 в репо:
   `pair.rs` — `WHITE_WINS_Y = 0.179` (вывод из WCAG `(Y+0.05)² < 1.05·0.05` ⇒
   `Y < 0.17913`; сосед `BLACK_WINS_Y = 0.1795`; комментарий «#007AFF
   (0.211 → 0.179)»); `semantic.rs` — симметричный WCAG-кроссовер маржи
   `Y ≈ 0.1791` и полоса двойной легальности `(0.1791, 0.1833]`; ADR-0003 —
   п. 3 открытых вопросов. Это граница полярности ТЕКСТА — другой вопрос, чем
   сторона пары: разрыв между порогами `0.30 − 0.17913 ≈ 0.121` ожидаем и
   ошибкой не является (0.30 решает сторону пары по каноническому якорю
   семьи; 0.179 — клапан WCAG-достижимости заливки под белым лейблом).
   **Открытый вопрос:** величина-«расхождение», равная 0.179, в репо не
   найдена (grep «0.179» даёт только перечисленные места). Ближайший
   кандидат — зазор между перцептивным переломом LPC (~0.36) и
   WCAG-переломом маржи (0.1791): ≈ 0.18 при номиналах репо, но точного
   0.179 из зафиксированных чисел не получается. Если внешние материалы
   имеют в виду именно дельту 0.179 — её первоисточник вне репо;
   интерпретацию не выдумываем (политика ADR-0002).
4. **Перелинковка.** Полнотекстовые обзоры ближайших работ по
   polarity-кроссоверу (задача #62) на момент правки в репо НЕ обнаружены
   (проверены история main и всех веток, тела PR, `docs/`, `experiments/`) —
   ссылку добавить, когда итоги #62 будут закоммичены. Запущенный
   эмпирический инструмент: PR #170 (см. выше).

### 2. `HUE_DRIFT_PENALTY_SLOPE` = 0.15 — приоритет ВЫСОКИЙ (14.29%); конфликт вывода РАЗРЕШЁН

**Конфликт вывода — РАЗРЕШЁН 2026-07-06 (кандидат отклонён по замеру):** строгий
кандидат — перцептивная цена дрейфа = длина хорды Oklab `2*C*sin(dh/2)`, около
`C*dh*pi/180`, откуда `penalty_scale = C*pi/180`, около 0.0026-0.0035 на градус
при C около 0.15-0.20, против текущего `slope/half_window = 0.15/30 = 0.005` на
градус. Замер на 49-якорном паспорте (l-сетка 0.05-0.95, шаг 0.01): прод-наклон
держит интерьерный оптимум на всех 43 хроматических якорях (0 прижатий к ребру);
хордовый кандидат прижимает оптимум к ребру окна ±30° на 12/43 якорях, флипает
27/43 (сдвиги до полного окна, dE_ok до 0.077). То есть кандидат не
объективизирует, а передаёт решение произвольной границе
`HUE_SEARCH_HALF_WINDOW` — интерьерный оптимум вырождается в клип по окну, и
нечувствительная константа окна становится чувствительной. Значение 0.15
остаётся калибровочным классом (d). Замок:
`derivation_rejection_locks::chord_derived_slope_rejected_degenerates_to_window_edge`
(scale.rs).
- **Если скаляр остаётся:** N >= 15, наблюдатели оценивают приемлемость оттенка
  акцентной рампы при разных штрафах дрейфа по L; подгонка компромисса
  «хрома против верности каноническому оттенку» пер-семья.
- **Критерий:** наклон, минимизирующий жалобы на дрейф при сохранении хромы.

### 3. `DECORATIVE_FLOOR_MIN` = 7.5 / `IC_DECORATIVE_FLOOR_MIN` = 15.0 — РАЗРЕШЕНО (`science/floor-derivation`, 2026-07-06)

Психофизика НЕ требуется — закрыто деривацией + грундингом:

- **`DECORATIVE_FLOOR_MIN` = 7.5 → (a) DERIVED** = `MODEL_LC_FLOOR` + `QUANT_GUARD`.
  `MODEL_LC_FLOOR = (LO_CLIP − LO_BOW_OFFSET) × LC_SCALE = (0.1 − 0.027) × 100 = 7.3`
  — минимальный ненулевой контраст, который вообще эмитит модель: за клипом
  `contrast_core` стартует ровно с 7.3, внутри клипа — ноль (issue #44). Полярности
  симметричны (`LO_WOB_OFFSET == LO_BOW_OFFSET`, `polarity_offsets_are_symmetric`;
  иначе floor = минимум по полярностям). Алгебраическая идентичность из GROUNDED
  APCA `0.0.98G-4g` набора → отдельная (a)-строка инвентаря #54 (литерал 7.3,
  тождество с формулой пиннится).
  - **Скан-замер** (`no_pair_emits_contrast_below_model_floor`): по квантованным
    8-бит sRGB-парам НИ ОДНА не эмитит контраст в (0, 7.3); фактический минимум
    ненулевого |Lc| = **7.3005** (0.0005 над инфимумом 7.3), скачок |Lc| одного
    8-бит шага у клипа = **7.85** (жёсткий разрыв).
  - **`QUANT_GUARD` = 0.2 → (c) EMPIRICAL** (новая строка #53). Гипотеза «0.2
    покрывает наблюдаемый шаг» ОТКЛОНЕНА замером (шаг 7.85, не 0.2); но guard'у это
    и не нужно — он держит цель (7.5) выше инфимума 7.3, чтобы квантование не
    уронило её в ноль; цель достижима в пределах `QUANT_BUDGET` = 1.0. Доминантный
    член 7.3 всё равно DERIVED.
  - Значение 7.5 — литерал (байт-идентичность; `7.3 + 0.2` в f64 ≠ 7.5 на ~1e-15),
    идентичность на компайл-тайме `const _` + `decorative_floor_is_model_floor_plus_guard`.

- **`IC_DECORATIVE_FLOOR_MIN` = 15.0 → (b) GROUNDED.** Опубликованный APCA-уровень
  Lc 15 — «absolute minimum for any non-text that needs to be discernible» (точка
  невидимости ниже неё): APCA project docs, Somers/Myndex (git.apcacontrast.com
  `WhyAPCA`, `APCA_in_a_Nutshell`). ⚠️ DRAFT/beta, single-origin, НЕ норматив
  WCAG 3 — заземление честно помечено как черновиковый дизайн-уровень, не
  независимое психофизическое измерение. IC-сдвиг +7.5 (= 15 − 7.5)
  порядкосохраняющий, пин `ic_floor_is_apca_lc15_with_order_preserving_shift`.

### 4. `HUE_PURITY_EXPONENT` = 0.6 / `HUE_PURITY_MP_REF_RATIO` = 1.5 — РАЗРЕШЕНО как (c) (`science/floor-derivation`, 2026-07-06)

Провенанс ФОРМЫ заземлён; магнитуда — калибровка, но её точное значение
нематериально для хроматического выхода:

- **Форма** «не доверяй тону у нейтрали» мотивирована ДВУМЯ сходящимися причинами:
  (1) численной — `atan2(b,a)` ill-conditioned у серой оси; (2) перцептивной —
  эффект Abney: тон монохроматического стимула сдвигается при разбавлении белым.
  Abney (1909) Proc. R. Soc. Lond. A **83**, 120–127, DOI 10.1098/rspa.1909.0085;
  величина сдвига растёт с падением колориметрической чистоты — Kurtenbach,
  Sternheim & Spillmann (1984) JOSA A **1(4)**, 365–372, DOI 10.1364/JOSAA.1.000365.
- ⚠️ **Перцептивный Abney ОТДЕЛЁН от численного atan2-шума.** Конкретные 0.6/1.5
  НЕ выведены из данных Abney — кривая эффект Abney не моделирует (issue #27,
  `abney_correct`); Abney даёт направление коррекции, не магнитуды.
- **Sensitivity (почему (c), а не (d)):** хроматические якоря (`mp ≥ mp_ref`) дают
  `purity = 1` при ЛЮБОМ показателе (инвариантны) — константы двигают ТОЛЬКО
  оттенок near-нейтралей, который и так atan2-шум. Свип показателя [0.4, 0.9] даёт
  max|Δpurity| = **0.148** — ограниченный непрерывный дрейф, не бинарный флип. Локи
  `hue_purity_curve_shape_is_pinned`, `exposure_hue_purity_curve`.
- Полная Abney-подгонка магнитуды (2AFC hue-shift, N ≥ 15) остаётся
  ОПЦИОНАЛЬНОЙ: хроматический выход стабилен, приоритет ниже (d)-мишеней #1/#2.

## Коррекции провенанса, найденные волной

1. **`ACHROMATIC_CHROMA_EPS` (строка 38):** реестр заявлял f64-шум конвейера
   «меньше 1e-12». ЗАМЕР: потолок хромы истинно-серых (R=G=B) = **3.7e-8**.
   EPS=1e-7 сидит лишь **около 2.7x** выше потолка шума (не «>=4 порядка»); запас
   >=3 порядка держится ТОЛЬКО сверху (мин. представимая хрома 1.06e-3).
   Классификация корректна (EPS между границами), но нижняя полоса ТОНКАЯ — строка
   исправлена.
2. **`HUE_DRIFT_PENALTY_SLOPE` (строка 37):** derivation-конфликт хорды Oklab
   разрешён 2026-07-06 — кандидат отклонён по замеру (вырождение в клип по ребру
   окна на 12/43 якорях, см. мишень #2); значение не тронуто, отказ запинован
   замком `derivation_rejection_locks::chord_derived_slope_rejected_degenerates_to_window_edge`.
