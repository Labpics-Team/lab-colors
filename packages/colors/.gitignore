# Generated WASM and private build receipts. Rebuilt by `npm run build`
# (and in CI) from their Rust sources; never hand-edited, never committed.
pkg/
private-program/labcolors_private_program.wasm
private-program/build-metadata.json
private-program/.build-receipt.json

# Local tooling
node_modules/
