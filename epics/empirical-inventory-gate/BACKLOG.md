# empirical-inventory-gate — BACKLOG (owner requests, verbatim-anchored)
Any executor that reads this file MUST drain `[OPEN]` items each grounding/NextWork cycle and on resume: promote → resolve with 0 questions → mark `[DONE]` + link PR. A non-empty backlog means the epic is NOT complete.

_(empty — no deferred owner requests yet)_
